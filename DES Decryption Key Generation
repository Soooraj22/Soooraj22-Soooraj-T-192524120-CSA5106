#include <stdio.h>

int main() {
    int keys[16], i;

    printf("Enter 16 round keys:\n");
    for(i = 0; i < 16; i++)
        scanf("%d", &keys[i]);

    printf("\nKeys for Decryption:\n");

    for(i = 15; i >= 0; i--)
        printf("K%d = %d\n", 16 - i, keys[i]);

    return 0;
}
